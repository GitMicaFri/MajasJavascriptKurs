// deklarera nummer
let myAge = 28;

// deklarera strängar
let myCarType = "Saab"

// deklarera en boolean
let myFirstBoolean = true
let mySecondBoolean = false

// deklarera en array
let myFruits = ['pear', 32, true, 'horse']

// deklarera ett objektvärde
// propertys med värden på
// : istället för = när vi är inne i ett objekt
let myPlant = {
    name: "Jimmy Plant", // name = property, Jimmy Plant = value
    sort: "Cactus",
    age: 2
}
// if-sats
if (myCarType === 'Saab') {
    // code code
} else {
    // vad händer annars?
}